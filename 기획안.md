# 펌프 공정품질현황 관리 앱 기획안

## 1. 프로젝트 개요

### 1.1 프로젝트명

**펌프 공정품질현황 관리 시스템**

### 1.2 프로젝트 목적

펌프 생산공정에서 모델별 검사기준과 검사결과를 체계적으로 관리하고, 검사 완료 후 공정품질현황을 자동으로 분석하여 보고서로 출력할 수 있는 웹 기반 품질관리 앱을 구축한다.

기존에 수작업으로 관리하던 검사결과를 다음과 같이 자동화한다.

> 모델 선택 → 검사일 선택 → 모델별 검사기준 자동 적용 → 시료 데이터 입력 → 자동 OK/NG 판정 → 검사결과 저장 → 품질통계 계산 → 품질현황 보고서 생성 → PDF 저장/메일/인쇄

### 1.3 기본 구축환경

| 구분     | 적용 기술                     |
| ------ | ------------------------- |
| 데이터 저장 | Google Sheets             |
| 앱      | Web App                   |
| 서버/로직  | Google Apps Script        |
| 화면     | HTML / CSS / JavaScript   |
| 기준정보   | Google Sheets `pump_spec` |
| 검사결과   | 모델명_YYYYMMDD 형식의 시트       |
| 보고서    | HTML 기반 A4 보고서            |
| PDF    | Google Apps Script PDF 변환 |
| 이메일    | Gmail/MailApp             |
| 인쇄     | 브라우저 인쇄 기능                |

---

# 2. 전체 시스템 구조

## 2.1 데이터 흐름

```text
[Google Sheets]
      │
      ▼
[pump_spec]
모델별 기준정보
전류/전력/유량
하한/상한
      │
      ▼
[웹앱]
모델 선택
검사일 선택
      │
      ▼
기준값 자동 표시
      │
      ▼
시료 1 입력
전류 → 전력 → 유량
      │
      ▼
시료 2 입력
전류 → 전력 → 유량
      │
      ▼
       ...
      │
      ▼
[입력완료]
      │
      ▼
자동 OK / NG 판정
      │
      ▼
[Google Sheets]
MODEL_YYYYMMDD
      │
      ▼
[보고서 생성]
      │
      ├── 검사수
      ├── 불량수
      ├── 불량율
      ├── 특성치별 Histogram
      ├── 상한/하한
      ├── CP
      └── Cpk
      │
      ├── PDF 저장
      ├── 이메일 전송
      └── 인쇄
```

---

# 3. Google Sheets 데이터 구조

## 3.1 `pump_spec` 시트

모델별 검사기준을 관리하는 핵심 기준정보 시트이다.

### 권장 구조

| 모델명     | 전류 하한 | 전류 상한 | 전력 하한 | 전력 상한 | 유량 하한 | 유량 상한 |
| ------- | ----: | ----: | ----: | ----: | ----: | ----: |
| MODEL_A |  1.20 |  1.80 |   250 |   350 |  10.0 |  15.0 |
| MODEL_B |  1.50 |  2.10 |   300 |   400 |  12.0 |  18.0 |
| MODEL_C |  2.00 |  2.50 |   400 |   500 |  15.0 |  20.0 |

### 설계 원칙

* 모델 1개당 1행
* 모델명은 중복되지 않도록 관리
* 전류/전력/유량별 하한값과 상한값 저장
* 향후 모델 추가 가능
* 기존 모델의 기준값 수정 가능
* 웹앱에서 실행할 때 최신 `pump_spec` 데이터를 읽어옴

---

# 4. 향후 확장을 고려한 기준정보 관리

단순히 모델명과 6개의 숫자만 저장하는 것보다 향후 품질관리 시스템으로 확장할 수 있도록 다음 항목을 추가할 수 있다.

| 필드    | 설명            |
| ----- | ------------- |
| 모델명   | 펌프 모델명        |
| 전류 하한 | 전류 최소 허용값     |
| 전류 상한 | 전류 최대 허용값     |
| 전력 하한 | 전력 최소 허용값     |
| 전력 상한 | 전력 최대 허용값     |
| 유량 하한 | 유량 최소 허용값     |
| 유량 상한 | 유량 최대 허용값     |
| 단위    | A / W / mLPM 등 |
| 사용여부  | 사용/미사용        |
| 등록일   | 기준정보 등록일      |
| 수정일   | 기준정보 수정일      |

초기 버전에서는 필수항목만 사용하고, 향후 관리기능을 확장할 수 있다.

---

# 5. 검사 입력 화면 기획

## 5.1 화면 구성

### 상단

**펌프 공정품질 검사**

```text
┌─────────────────────────────────────┐
│             펌프 공정품질 검사       │
├─────────────────────────────────────┤
│ 모델명  [ MODEL_A ▼ ]               │
│ 검사일  [2026] [09] [22]            │
├─────────────────────────────────────┤
│             검사 기준                │
│ 전류 : 1.20 ~ 1.80 A                │
│ 전력 : 250 ~ 350 W                  │
│ 유량 : 10.0 ~ 15.0 ml/min                  │
└─────────────────────────────────────┘
```

---

# 6. 모델명 선택

모델명은 `pump_spec` 시트에 등록되어 있는 모델 목록을 자동으로 불러온다.

예:

```text
모델명
[ MODEL_A ▼ ]
```

모델을 선택하면 해당 모델의 기준값을 자동으로 불러온다.

예:

```text
전류
하한 : 1.20
상한 : 1.80

전력
하한 : 250
상한 : 350

유량
하한 : 10.0
상한 : 15.0
```

사용자가 직접 하한/상한을 입력하지 않도록 하여 잘못된 기준값 입력을 방지한다.

---

# 7. 검사일 선택

년도 / 월 / 일을 각각 선택하는 스핀버튼 형태로 구성한다.

```text
검사일

[2026 ▲▼] 년
[09 ▲▼] 월
[22 ▲▼] 일
```

또는 모바일/PC 환경에 따라 날짜 선택 UI를 사용할 수 있다.

선택된 날짜는 내부적으로 다음 형식으로 변환한다.

```text
YYYYMMDD
```

예:

```text
2026년 9월 22일
        ↓
20260922
```

---

# 8. 시료 검사 데이터 입력

검사기준 아래에 시료 입력 영역을 배치한다.

## 8.1 시료 1

```text
┌─────────────────────────────────────┐
│ 시료 1                               │
│                                     │
│ 전류 [          ] A                 │
│ 전력 [          ] W                 │
│ 유량 [          ] ml/min                  │
└─────────────────────────────────────┘
```

전류 → 전력 → 유량 순서로 입력한다.

전류 입력 후 Enter 또는 다음 버튼을 누르면 전력 입력으로 이동하고,

전력 입력 후 유량 입력으로 이동한다.

유량 입력이 완료되면 자동으로 다음 시료 입력영역이 생성되거나 활성화된다.

---

# 9. 연속 시료 입력 방식

사용자가 검사 시료 수를 미리 지정하지 않아도 입력을 계속할 수 있도록 구성한다.

예:

```text
시료 1
전류 [1.52]
전력 [302]
유량 [13.2]

시료 2
전류 [1.48]
전력 [310]
유량 [13.8]

시료 3
전류 [1.56]
전력 [298]
유량 [12.9]

        ...

시료 20
전류 [1.62]
전력 [315]
유량 [14.1]
```

화면 하단에는

**[입력완료]**

버튼을 배치한다.

---

# 10. 입력 데이터 검증

입력 단계에서 다음 검증을 수행한다.

### 숫자 검증

전류, 전력, 유량은 숫자만 입력 가능하도록 한다.

### 빈 값 검증

```text
전류 미입력
→ "전류값을 입력해주세요."
```

### 입력 순서 검증

```text
전류
 ↓
전력
 ↓
유량
 ↓
다음 시료
```

### 중복/누락 방지

유량까지 입력되지 않은 시료가 있는 상태에서 `입력완료`를 누르면 완료되지 않도록 한다.

---

# 11. 검사결과 판정 로직

각 시료에 대해 전류, 전력, 유량을 각각 검사한다.

기준값:

```text
하한값 ≤ 측정값 ≤ 상한값
```

이면 해당 특성치는 정상이다.

예:

```text
전류 기준 : 1.20 ~ 1.80

측정값 1.50 → OK
측정값 1.20 → OK
측정값 1.80 → OK
측정값 1.10 → NG
측정값 1.90 → NG
```

---

# 12. 시료별 최종 판정

한 시료의 전류, 전력, 유량 중 하나라도 기준을 벗어나면 해당 시료는 `NG`로 판정한다.

### 판정 로직

```text
전류 OK
AND
전력 OK
AND
유량 OK
        ↓
       OK
```

하나라도 NG이면

```text
전류 NG
OR
전력 NG
OR
유량 NG
        ↓
       NG
```

따라서 최종 판정은 **시료 단위**로 관리한다.

---

# 13. 검사결과 저장 시트

`입력완료` 버튼을 누르면 Google Sheets에 검사결과를 저장한다.

시트명은 다음 규칙으로 생성한다.

```text
모델명_YYYYMMDD
```

예:

```text
MODEL_A_20260922
```

이미 동일한 이름의 시트가 존재하는 경우에는 중복 저장 정책을 별도로 적용한다.

권장 방식은:

* 동일 모델/날짜 검사 결과가 이미 있으면 기존 데이터에 추가
* 또는 검사 차수/LOT 번호를 추가하여 별도 관리

향후 생산현장 사용을 고려하면 **동일 모델/날짜에 여러 번 검사할 가능성**이 있으므로 이 부분은 실제 운영방식에 맞춰 결정하는 것이 좋다.

---

# 14. 검사결과 시트 구조

예:

### `MODEL_A_20260922`

| 시료번호 |  전류치 | 전력치 |   유량 | 판정 |
| ---: | ---: | --: | ---: | -- |
|    1 | 1.52 | 302 | 13.2 | OK |
|    2 | 1.48 | 310 | 13.8 | OK |
|    3 | 1.92 | 305 | 14.0 | NG |
|    4 | 1.56 | 298 | 12.9 | OK |
|    5 | 1.61 | 355 | 13.5 | NG |

상단에는 해당 검사에 사용된 기준정보를 함께 저장하는 것을 권장한다.

예:

```text
모델명 : MODEL_A
검사일 : 2026-09-22

전류 하한 : 1.20
전류 상한 : 1.80

전력 하한 : 250
전력 상한 : 350

유량 하한 : 10.0
유량 상한 : 15.0
```

이렇게 저장하면 나중에 `pump_spec`의 기준값이 변경되어도 **과거 검사 당시의 기준값을 그대로 보존**할 수 있다.

---

# 15. 숫자 표시 규칙

### 전류

소수점 이하 2자리

```text
1.52
1.48
2.10
```

### 전력

기획 단계에서는 원 데이터의 단위와 필요한 소수점 자릿수를 설정할 수 있도록 구성한다.

### 유량

현장 측정기의 표시단위에 맞춰 설정한다.

### 불량율

소수점 이하 4자리까지 표시한다.

예:

```text
5.2632%
```

내부 계산은 반올림 전의 원 데이터를 사용하고, 화면 표시 단계에서 반올림하는 방식으로 한다.

---

# 16. 공정품질현황 보고서

검사 완료 후 앱에서

**[보고서출력]**

버튼을 제공한다.

버튼을 누르면 해당 모델/검사일의 데이터를 기반으로 품질보고서를 생성한다.

---

# 17. 보고서 제목

예:

```text
MODEL_A 공정 품질현황
```

보고서 상단에 다음 정보를 표시한다.

```text
펌프 공정 품질현황

모델명 : MODEL_A
검사일 : 2026-09-22
```

---

# 18. 검사결과 요약

보고서 상단에 품질현황을 한눈에 확인할 수 있도록 요약정보를 표시한다.

예:

| 항목    |      결과 |
| ----- | ------: |
| 총 검사수 |     100 |
| 불량수   |       5 |
| 불량율   | 5.0000% |
| 정상수   |      95 |

불량율 계산:

```text
불량율 = 불량수 / 총 검사수 × 100
```

---

# 19. 특성치별 품질분석

각 특성치별로 별도의 분석영역을 구성한다.

## 전류

```text
전류 품질분석

상한 : 1.80
하한 : 1.20

       │       ███
       │    ███████
       │ ███████████
       └────────────────
         1.2        1.8
```

히스토그램에는 다음 정보를 표시한다.

* 측정값 분포
* 하한선
* 상한선
* 평균값
* 필요시 표준편차
* NG 영역

---

# 20. 전력 품질분석

전력 역시 동일한 형태로 표시한다.

```text
전력 품질분석

상한 : 350
하한 : 250

[히스토그램]
```

---

# 21. 유량 품질분석

```text
유량 품질분석

상한 : 15.0
하한 : 10.0

[히스토그램]
```

---

# 22. 특성치별 불량율

각 특성치에 대해서도 별도의 불량률을 산출한다.

예:

| 특성치 | 불량수 |     불량율 |
| --- | --: | ------: |
| 전류  |   2 | 2.0000% |
| 전력  |   3 | 3.0000% |
| 유량  |   1 | 1.0000% |

전체 시료 판정의 불량율과 특성치별 불량율을 구분한다.

---

# 23. CP / Cpk 분석

각 특성치별로 공정능력을 계산한다.

## Cp

양측 규격이 있는 경우 다음과 같이 계산한다.

```text
Cp = (USL - LSL) / (6σ)
```

* USL = 상한규격
* LSL = 하한규격
* σ = 표준편차

## Cpk

```text
Cpk =
MIN(
(USL - 평균) / (3σ),
(평균 - LSL) / (3σ)
)
```

보고서에는 다음과 같이 표시한다.

| 특성치 |   하한 |   상한 |   평균 | 표준편차 | Cp | Cpk |
| --- | ---: | ---: | ---: | ---: | -: | --: |
| 전류  | 1.20 | 1.80 | 1.52 |    - |  - |   - |
| 전력  |  250 |  350 |  302 |    - |  - |   - |
| 유량  | 10.0 | 15.0 | 13.2 |    - |  - |   - |

### 주의사항

검사 시료 수가 충분하지 않거나 표준편차가 0인 경우 CP/Cpk를 정상적으로 계산할 수 없으므로 다음과 같이 표시한다.

```text
N/A
```

또한 CP/Cpk는 계산 방식에 따라 결과가 달라질 수 있으므로 실제 품질관리 기준에서 사용할 **표준편차 계산 방식(STDEV.S 등)과 최소 시료수 기준**을 개발 전에 확정하는 것이 좋다.

---

# 24. 보고서 A4 레이아웃

보고서는 A4 세로 방향을 기본으로 설계한다.

## 1페이지

```text
┌──────────────────────────────┐
│      MODEL_A 공정 품질현황    │
│                              │
│ 검사일 / 모델명              │
├──────────────────────────────┤
│ 검사수 │ 불량수 │ 불량율      │
├──────────────────────────────┤
│                              │
│       전류 Histogram          │
│                              │
├──────────────────────────────┤
│       전력 Histogram          │
│                              │
├──────────────────────────────┤
│       유량 Histogram          │
│                              │
└──────────────────────────────┘
```

## 2페이지

```text
┌──────────────────────────────┐
│       공정능력 분석           │
├──────────────────────────────┤
│ 전류                         │
│ 하한 / 상한 / 평균 / Cp/Cpk  │
├──────────────────────────────┤
│ 전력                         │
│ 하한 / 상한 / 평균 / Cp/Cpk  │
├──────────────────────────────┤
│ 유량                         │
│ 하한 / 상한 / 평균 / Cp/Cpk  │
├──────────────────────────────┤
│ 특성치별 불량율              │
└──────────────────────────────┘
```

실제 데이터 양에 따라 1~2페이지 또는 그 이상의 페이지로 자동 분할한다.

---

# 25. 보고서 출력 화면

보고서 화면에는 다음 버튼을 제공한다.

```text
┌───────────────────────────────┐
│     MODEL_A 공정 품질현황      │
│                               │
│       [품질분석 보고서]        │
│                               │
│       [ 저장 ] [ 인쇄 ]       │
└───────────────────────────────┘
```

---

# 26. 저장 기능

`저장` 버튼을 누르면 보고서를 PDF로 변환한다.

PDF 파일명은 검사결과가 저장된 시트명을 기준으로 한다.

예:

```text
MODEL_A_20260922
```

↓

```text
MODEL_A_20260922_공정품질.pdf
```

또는 사용자 요구에 맞춰

```text
MODEL_A_20260922공정품질.pdf
```

형태로 생성할 수 있다.

권장 파일명:

**`MODEL_A_20260922_공정품질.pdf`**

가독성과 파일 관리 측면에서 유리하다.

---

# 27. PDF 이메일 전송

PDF 생성이 완료되면 지정된 이메일 주소로 전송한다.

수신자:

```text
kik1959@naver.com
```

메일 제목 예:

```text
[펌프 공정품질현황] MODEL_A_20260922
```

첨부파일:

```text
MODEL_A_20260922_공정품질.pdf
```

메일 본문에는 다음 정보를 표시한다.

```text
펌프 공정품질현황 보고서입니다.

모델명 : MODEL_A
검사일 : 2026-09-22
총 검사수 : 100
불량수 : 5
불량율 : 5.0000%
```

---

# 28. 인쇄 기능

`인쇄` 버튼을 누르면 A4 인쇄용 레이아웃을 적용한다.

브라우저의 인쇄 기능을 활용하여

```text
A4
세로
여백 설정
머리글/바닥글 제어
페이지 분할
```

등을 적용한다.

웹 화면용 CSS와 인쇄용 CSS를 분리한다.

예:

```text
일반 화면
→ 화면에 최적화

인쇄
→ A4 용지에 최적화
```

인쇄 시 버튼이나 입력창 등 불필요한 UI는 자동으로 숨긴다.

---

# 29. 화면 메뉴 구조

전체 앱은 다음과 같은 구조를 권장한다.

```text
펌프 공정품질 관리
│
├── 검사입력
│   ├── 모델 선택
│   ├── 검사일 선택
│   ├── 기준값 확인
│   ├── 시료 입력
│   └── 입력완료
│
├── 품질보고서
│   ├── 검사결과
│   ├── Histogram
│   ├── 불량율
│   ├── Cp
│   ├── Cpk
│   ├── PDF 저장
│   └── 인쇄
│
└── 기준정보
    └── 모델/규격 관리
```

초기 버전에서는 `기준정보` 메뉴를 관리자용으로 제한할 수 있다.

---

# 30. 모델 추가 기능

향후 사용자가 직접 모델을 추가할 수 있도록 한다.

예:

```text
[모델 추가]

모델명       [ MODEL_D ]

전류 하한   [ 1.20 ]
전류 상한   [ 1.80 ]

전력 하한   [ 250 ]
전력 상한   [ 350 ]

유량 하한   [ 10.0 ]
유량 상한   [ 15.0 ]

        [저장]
```

저장하면 자동으로 `pump_spec`에 새로운 행이 생성된다.

따라서 프로그램 코드를 수정하지 않고도 새로운 모델을 추가할 수 있다.

---

# 31. 모델 수정/삭제

관리자 기능으로 다음 기능을 제공할 수 있다.

```text
모델 검색
   ↓
MODEL_A
   ↓
[수정]
[사용중지]
```

실제 운영에서는 모델을 완전히 삭제하기보다는 `사용여부`를 두어 과거 검사기록과의 연결을 유지하는 방식을 권장한다.

---

# 32. 데이터 무결성

품질관리 앱인 만큼 다음 사항을 중요하게 관리한다.

### 기준값 저장

검사 결과 저장 당시의

* 모델명
* 검사일
* 전류 하한/상한
* 전력 하한/상한
* 유량 하한/상한

을 검사결과 시트에 함께 기록한다.

### 이유

향후 `pump_spec`의 기준값이 변경될 수 있기 때문이다.

예를 들어,

```text
2026년 9월
전류 기준 1.20 ~ 1.80

2027년 1월
전류 기준 1.30 ~ 1.90
```

로 변경될 경우 2026년 검사결과가 새로운 기준으로 다시 계산되는 문제가 발생하면 안 된다.

---

# 33. 오류 처리

다음 상황에 대한 오류 메시지를 제공한다.

### 모델 미선택

```text
검사할 펌프 모델을 선택해주세요.
```

### 검사일 미선택

```text
검사일을 선택해주세요.
```

### 시료 미입력

```text
검사 데이터를 입력해주세요.
```

### 숫자가 아닌 값

```text
전류/전력/유량은 숫자로 입력해주세요.
```

### Google Sheets 연결 오류

```text
데이터 저장 중 오류가 발생했습니다.
잠시 후 다시 시도해주세요.
```

### PDF 생성 오류

```text
보고서 PDF 생성 중 오류가 발생했습니다.
```

---

# 34. 권장 데이터 구조

향후 시스템 확장을 고려하면 Google Sheets를 다음과 같이 구성하는 것을 권장한다.

### Sheet 1 : `pump_spec`

모델별 기준정보

```text
모델명
전류 하한
전류 상한
전력 하한
전력 상한
유량 하한
유량 상한
사용여부
수정일
```

### Sheet 2 : `app_config`

시스템 설정

```text
회사명
보고서 제목
이메일 수신자
단위
PDF 파일명 규칙
```

### Sheet 3 : 모델별 검사결과

```text
MODEL_A_20260922
MODEL_A_20260923
MODEL_B_20260922
...
```

---

# 35. 개발 구조

Google Apps Script 프로젝트는 다음과 같이 구성한다.

```text
Code.gs
│
├── doGet()
├── getPumpModels()
├── getPumpSpec()
├── saveInspectionData()
├── calculateResult()
├── getReportData()
├── calculateStatistics()
├── createHistogram()
├── generatePdf()
├── sendPdfEmail()
└── printReport()

index.html
│
├── 검사 입력 화면
├── 모델 선택
├── 날짜 선택
├── 시료 입력
└── 보고서 화면

style.css
│
├── 화면 디자인
├── 모바일 대응
└── A4 인쇄 CSS

script.html
│
├── 모델 데이터 처리
├── 시료 입력 처리
├── API 호출
├── 판정
└── 보고서 렌더링
```

---

# 36. 사용자 화면 UX

현장 작업자가 빠르게 입력할 수 있도록 화면을 단순하게 구성한다.

### 원칙

1. 버튼을 크게 구성
2. 입력 순서를 명확하게 표시
3. 숫자 입력에 최적화
4. 모델 선택 후 기준값 자동 표시
5. 입력 중 기준값을 항상 확인 가능
6. 시료 입력을 끊김 없이 연속 진행
7. 입력완료 전 최종 확인
8. 저장 완료 후 명확한 메시지 제공

---

# 37. 입력완료 전 확인창

사용자가 `입력완료`를 누르면 바로 저장하지 않고 최종 확인창을 표시하는 것을 권장한다.

예:

```text
검사결과를 저장하시겠습니까?

모델명 : MODEL_A
검사일 : 2026-09-22
총 검사수 : 100
예상 불량수 : 5

[취소]       [저장]
```

저장을 누르면 Google Sheets에 기록한다.

---

# 38. 저장 완료 화면

저장 완료 후:

```text
검사결과 저장이 완료되었습니다.

모델명 : MODEL_A
검사일 : 2026-09-22
검사수 : 100
불량수 : 5
불량율 : 5.0000%

[보고서출력]
```

버튼을 바로 제공한다.

---

# 39. 보고서의 핵심 정보

최종 보고서는 다음 정보를 포함한다.

### 기본정보

* 모델명
* 검사일
* 검사수

### 품질현황

* 총 검사수
* 정상수
* 불량수
* 전체 불량율

### 특성치별 분석

* 전류 Histogram
* 전력 Histogram
* 유량 Histogram
* 각 특성치의 하한
* 각 특성치의 상한
* 평균
* 표준편차
* 특성치별 불량수
* 특성치별 불량율
* Cp
* Cpk

### 출력

* 화면보기
* PDF
* 이메일
* 인쇄

---

# 40. 향후 확장 기능

1차 버전 이후 다음 기능을 추가할 수 있다.

### 기간별 품질조회

```text
2026년 9월
MODEL_A
검사수
불량수
불량율
```

### 모델별 품질비교

```text
MODEL_A
MODEL_B
MODEL_C
```

### 월별 품질추이

```text
1월 → 2월 → 3월 → ...
```

### 불량 원인관리

현재는 판정만 관리하지만 향후

```text
NG
 ├── 전류 초과
 ├── 전류 미달
 ├── 전력 초과
 ├── 전력 미달
 ├── 유량 초과
 └── 유량 미달
```

형태로 세분화할 수 있다.

### LOT 관리

```text
LOT 번호
생산라인
검사자
설비번호
```

등을 추가할 수 있다.

---

# 41. 최종 시스템 사용 시나리오

## STEP 1

작업자가 웹앱을 실행한다.

## STEP 2

모델명을 선택한다.

```text
MODEL_A
```

## STEP 3

검사일을 선택한다.

```text
2026 / 09 / 22
```

## STEP 4

시스템이 `pump_spec`에서 기준값을 자동으로 가져온다.

```text
전류 1.20 ~ 1.80
전력 250 ~ 350
유량 10.0 ~ 15.0
```

## STEP 5

시료 데이터를 순서대로 입력한다.

```text
시료 1
→ 전류
→ 전력
→ 유량

시료 2
→ 전류
→ 전력
→ 유량

...
```

## STEP 6

`입력완료`를 누른다.

## STEP 7

시스템이 각 시료를 자동 판정한다.

```text
시료 1 → OK
시료 2 → OK
시료 3 → NG
...
```

## STEP 8

Google Sheets에

```text
MODEL_A_20260922
```

시트를 생성/갱신한다.

## STEP 9

`보고서출력`을 누른다.

## STEP 10

품질현황 보고서를 표시한다.

```text
총 검사수
불량수
불량율

전류 Histogram
전력 Histogram
유량 Histogram

전류 Cp/Cpk
전력 Cp/Cpk
유량 Cp/Cpk
```

## STEP 11

`저장` 버튼을 누른다.

```text
MODEL_A_20260922_공정품질.pdf
```

PDF를 생성하여 지정 이메일로 전송한다.

## STEP 12

`인쇄` 버튼을 누르면 A4 인쇄용 레이아웃으로 출력한다.

---

# 42. 개발 시 반드시 확정할 사항

개발 착수 전에 다음 5가지는 확정하는 것이 좋다.

### ① 전력/유량의 표시 소수점

현재 요구사항에서는 전류와 전력은 소수점 2자리까지라고 되어 있으므로 전력도 2자리로 적용할지, 유량은 몇 자리까지 표시할지를 확정한다.

### ② CP/Cpk 최소 검사수

예를 들어 검사수가 너무 적을 경우 CP/Cpk를 표시하지 않고 `N/A`로 처리할 기준을 정한다.

### ③ 동일 모델/동일 날짜 재검사

예:

```text
MODEL_A_20260922
```

가 이미 존재할 때

* 기존 시트에 추가
* 기존 시트 덮어쓰기
* 검사번호를 추가하여 별도 시트 생성

중 어떤 방식을 사용할지 정한다.

### ④ 기준 변경 이력

`pump_spec`의 기준이 변경됐을 때 과거 검사결과에 영향을 주지 않도록 **검사 당시 기준값을 결과 시트에 복사하여 보관**한다.

### ⑤ 사용자 권한

향후에는

```text
작업자
관리자
```

를 구분하여

작업자는 검사입력만 가능하고 관리자는 `pump_spec`을 수정할 수 있도록 권한을 나누는 것도 가능하다.

---

# 43. 최종 목표

본 시스템은 단순한 검사값 입력 프로그램이 아니라,

**「펌프 모델별 검사기준 관리 → 현장 검사 → 자동 판정 → 데이터 축적 → 통계분석 → 공정능력 분석 → 품질보고서 → PDF/메일/인쇄」**

까지 연결되는 **펌프 공정품질관리 시스템**을 목표로 한다.

특히 Google Sheets를 데이터 저장소로 사용하기 때문에 모델과 검사기준을 추가하거나 수정할 때 프로그램을 다시 개발하지 않아도 되는 구조로 설계한다.
